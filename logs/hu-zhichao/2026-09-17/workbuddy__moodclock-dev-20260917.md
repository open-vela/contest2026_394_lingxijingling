
## 2026-09-17 07:50 屏幕自愈版 mood_clock 上板（黑屏复发根治）
- 在 mood_clock.ino loop() 插入自愈：ST_RUN 下每 30s setPower(true)+setBacklight(255)，防"固件存活但屏幕全黑"复发（此前 2 次均靠远程 BRIGHT 255 救活）。已知取舍：串口 BRIGHT 0 会在 30s 后被顶回。
- 编译烧录成功（BUILD_EXIT=0 / FLASH_EXIT=0），串口复验：stage2、WiFi 192.168.110.134、NTP 07:50、晨光主题自动切换、汝城 21.3C 96%（code d18，07:35 更新）、heap/psram 正常。
- 环境注意：Bash 工具的 dirname/cat/head 持续不可用，shell 操作一律走 PowerShell；PowerShell 输出中文会 mojibake，落盘文件用 ReadAllText+UTF8 转码后再读。

## 2026-09-17 08:05 早晨黑屏结案：两种故障叠加
- 故障A（07:52）：固件存活+背光已强制拉满但屏幕灰黑、只有微弱发光感 → 判"面板偏压轨掉电"假说：背光亮但面板电源/GVDD 没偏压，透过黑面板只见微光。GPIO15 周期再拉满救不回，疑面板电源 latched off 需完整断电。
- 故障B（08:00）：用户拔线重插后串口完全无响应（0 行回显）→ esptool 确认芯片卡在 ROM 下载模式（stub 能连上即证），esptool -p COM3 --after watchdog-reset run 救活，固件恢复 stage2/WiFi/08:05 晴昼主题。
- 关键教训：用户日常用 BOOT 键翻页，拔插线时若按住 BOOT → 进下载模式黑屏。恢复口诀=拔线 10s，重插时任何键都不要按；或 esptool watchdog-reset。
- 排查顺序沉淀：串口 0 回显 → 先 esptool 探芯片（能连=卡下载模式，watchdog-reset 即救）；有回显但屏黑 → BRIGHT255/INV reinit/TP 判决链。

## 2026-09-17 08:36 雪花屏处置：慢总线默认版上线
- 判决链：SB 直写硬件的色块也雪花 + 雪花"一闪一闪"（写入有到达但内容坏）→ 总线信号余量/面板供电波动嫌疑。
- 行动：SLOW/REINIT 串口命令入固件；tft.begin() 后默认 setSlowBus(true)（1us 级写入，整帧约0.9s/分钟一刷），编译烧录成功（08:36 INFO 全绿：stage2/WiFi/08:36 晴昼/22.2C）。
- 待用户做断电+换充电头供电观察：正常=供电质量问题；仍雪花=面板供电电路/面板硬件损坏（此日黑→灰→雪为逐步恶化轨迹）。若慢速也救不回则软件手段到极限，转硬件处理建议。
## 2026-09-17 08:45 屏幕故障终局判决：软件手段全部到极限
- mood_clock（快/慢 1us 总线、SB 直写、INV reinit）与 med_guard（独立代码、SCRSLOW 1us + SCRREINIT + SCRTEST）两套完全独立的写屏路径全部无法出内容：快总线的雪花 → 慢总线的灰屏，WR 低电平已给到面板要求 66 倍余量仍无效。
- 症状全天恶化轨迹：运行中黑(2次) → 灰黑带微光(偏压轨掉电嫌疑) → 雪花一闪一闪(写入到达但内容坏) → 换线/断电/重烧后持续灰屏。期间 08:05 watchdog 复位后曾短暂恢复过一次(用户确认'好了')，随后复发恶化 → 符合'接触不良/焊接裂纹/面板 IC 渐进损坏'特征而非纯软件。
- 最后物理判决实验：SCRTEST 让 8 色条持续播放，指导用户按压屏排线/板边观察内容是否闪现（区分排线接触 vs 面板本体），并做充电头供电冷启动。
- 传感器/马达/喇叭/时钟/天气数据链路全程健康，仅显示硬件通路失效。后续出路：按压若有反应=排线/焊点可救；无反应=面板或其供电电路损坏，走保修/换屏。
## 2026-09-17 08:52 按压判决结果：接触不良排除，进入长断电终判
- 按压测试：按排线出口/USB-C 附近电路板，画面完全无反应（仅按玻璃时指下泛白=LCD 正常物理反应）→ 排线松脱/焊点裂纹排除。
- 剩余嫌疑：面板驱动 IC/偏压轨闩死（短拔插放不干净电）或面板本体/总线 GPIO 硬件损伤。
- 终判实验：拔线静置 10 分钟放电 → 充电头冷启动。恢复=闩死类故障，以后注意避免长时间运行中热插拔；仍灰=面板硬件损坏，走保修/退换。
- 若恢复则重烧 mood_clock 双页版（时间+天气+心率 / 心情选色）。
## 2026-09-17 09:08 屏幕终判：硬件损坏（保修处理）
- 终判实验：10 分钟长断电放电 + 冷启动 → 开机即雪花、随后灰亮屏。闩死假说排除，故障已稳定化。
- 完整排除清单：两套独立固件(mood_clock/med_guard)、66倍时序余量(1us WR)、SB直写、INV0/1重初始化、按压排线/板面(无反应=非接触不良)、换线、短/长断电。全部失败。
- 轨迹：09-16 全天正常 → 晚间运行中黑屏2次(可复位) → 09-17 晨灰屏(复位后短暂恢复) → 雪花一闪一闪 → 稳定灰屏。渐进恶化=硬件损伤特征。
- 最可能：面板驱动 IC/偏压轨损坏，或 8080 总线某 GPIO(39/40/41/42/45/46/47/48) ESD 损伤(热插拔频繁)——两者外观表现相同，均不可软件修复。
- 处置：走保修/退换。非显示功能(WiFi/NTP/天气/摇晃/马达/喇叭)仍正常。换新板后烧 mood_clock(双页)+med_guard 固件即可分钟级恢复，固件与 build 产物均留在 workspace。
## 2026-09-17 09:30 ★大反转：屏幕没有坏！lcd_min 第三套驱动点亮成功
- 用户要求从零写驱动。lcd_min（纯 digitalWrite 手工并口、零依赖）烧录后：颜色循环成功显示（红绿蓝白黑+彩条）→ 面板/背光/总线全部正常，此前"硬件损坏"判决被推翻（此前失败应是面板状态/时序配置组合问题）。
- 遗留：固定左侧雪花条。根因=列偏移：ST7789 RAM 240 列，面板可见 170 列=RAM 35..204（正式驱动 s_colOff=35），lcd_min 从 0 开始写 → 35 像素宽区域永远写不到=固定雪花条。
- v3 修复：setWindow 的 x 坐标 +35（COLOFF），已编译烧录待用户确认。
- 教训：从零写驱动必须抄全三件套：MADCTL 方向位 + 列/行 RAM 偏移 + 偏压参数（0xC2/0xC3/0xC4）。偏移缺失不会让屏幕全黑，只会留一条固定垃圾条，极易误判为硬件坏。
- 下一步：确认雪花消除后，在 lcd_min 底座上重建双页 UI（或先把 mood_clock 的 TDisplayS3_ST7789 驱动换上 lcd_min 的手工并口写路径）。
## 2026-09-17 09:50 ★屏幕正式复活：mood_clock 换装慢速引擎上线
- 雪花条根因（列偏移 35 缺失）修复后，用户确认 lcd_min v3 画面干净无雪花。
- mood_clock/TDisplayS3_ST7789.cpp 引擎替换：busWrite 改为 digitalWrite 慢路径（8 脚 digitalWrite + 1us 建立 + 5us WR低 + 5us WR高），fillRect/pushImage 删除寄存器快路径分支，全部走 busWrite。窗口偏移（s_colOff/s_rowOff）本就正确。
- 编译烧录成功，串口全绿：WiFi 192.168.110.134、NTP 09:50:07、汝城 24.6C 多云 83% aqi39 d01。
- 代价：全帧约 2.5s 可见扫描，每分钟刷一次；传感器轮询 loop delay 2ms 在 2.5s 刷屏期间会被阻塞——摇晃检测在刷新瞬间可能丢拍，可接受（如需可后续把刷屏分块让出）。
- 复盘：上午"硬件损坏"判决错误，真实链条=面板对快时序敏感+驱动快路径参数不匹配+列偏移只在正式驱动里而有偏移的驱动走快路径坏。三套独立实现交叉验证的价值：lcd_min（慢+偏移全对）成为金标准，把引擎移植回去即修复。
## 2026-09-17 10:04 回滚：mood_clock 冷启动雪花未修完，先退回 lcd_min
- mood_clock 冷启动（重插 USB）后雪花，根因假设=上电 20ms 太短+初始化被吞，已在 TDisplayS3_ST7789.cpp 写了两处修复（powerOn delay 20→100ms；begin() 里 runInit 连跑两遍），**源码已改但未编译烧录**，修复保留待下次验证。
- 用户要求先回上一个固件：直接重烧 build_lcdmin（09:29 构建，用户确认过画面干净的版本），flash_fast.py --skip-compile，MAC A0:F2:62:E2:69:4C @ COM3，烧录+校验+看门狗复位全过，串口看到 lcd_min 色块循环（GREEN/BLUE/WHITE/BLACK/BARS）。
- 待办：mood_clock 的双保险初始化修复编译烧录，验证冷插不再雪花。
## 2026-09-17 10:08 mood_clock 带冷启动修复烧录成功
- 流程：用户先要求回退 lcd_min（已烧，正常），随后要求烧显示固件 → 编译烧录 mood_clock（含两处雪花修复：powerOn 20→100ms、runInit 双跑），BUILD_EXIT=0 FLASH_EXIT=0，第 2 次尝试成功。
- 串口全绿：WiFi 192.168.110.134、NTP 10:07:31、汝城 25.4C 多云 76% aqi39。
- 注意：本次是烧录后热启动，**冷插验证还没做**——下次拔插 USB 后看是否仍雪花，才能判定双保险初始化是否真正解决冷启动问题。
## 2026-09-17 10:20 ★冷启动雪花真凶落网：上电窗口 RST 浮空
- 双跑初始化+100ms 延时无效（10:06 版实测冷插仍雪花）→ 推翻"电轨爬升吞命令"假设。
- 真正根因（对比 lcd_min 上电顺序实锤）：lcd_min 先 pinMode 全部 LCD 脚（Arduino 输出默认低 → RST 天然压在复位态）再拉 GPIO15，面板带着干净 RST=0 上电；mood_clock 的 powerOn() 先拉供电再配脚，面板上电爬升的 100ms 里 RST/CS/DC/WR 全部浮空，面板靠内部上拉在脏总线+不稳电源上自行退出复位 → 内部状态损坏 → 后面重发多少遍初始化都雪花。热启动没事是因为各脚上一轮已被驱动、面板没断过电。
- 修复：TDisplayS3_ST7789.cpp begin() 里 powerOn() 之前先驱动全部脚（RST 低、CS/DC/WR/RD 高、数据低），再 powerOn()。受控释放交给 runInit 开头硬复位。
- 已编译烧录（build_moodclock5，10:19），串口全绿：fb ready、font 自检 OK、WiFi 192.168.110.134、NTP 10:19:43、汝城 26.0C 多云。**冷插复测待用户执行**。
- 经验：★并口屏（GPIO 供电使能类板子）上电次序铁律——先全部信号脚定电平（RST 必须压低）→ 再拉供电使能 → 再受控释放复位。任何"先供电后配脚"的写法都会留下浮空窗口。
## 2026-09-17 13:26 冷启动雪花终局方案上线（6 轮暴力重初始化环）
- 用户要求"每次开机固定画面"，且确认不是端口重复（驱动引脚表与 lcd_min 逐脚一致；外设 0/1/2/12 不碰 LCD 总线 39-48/5-9/15/38）。
- 新增三层保险：① begin() 先 delay(300) 等冷插电源稳定（lcd_min 有、mood_clock 缺的关键差异）；② 上电前全部 LCD 脚预驱动（RST 压低）；③ 初始化后 ID 自检 + 失败整轮断电 300ms 重上电再初始化，最多 6 轮——最后一轮必然落在电源稳定之后。
- 实测：ID 读回恒为 FF 81 81（面板实读签名，非教科书 85 80 52；且 .h 注释警告读回可能是电容回声，不作判决）→ 自检永远不过 → 每次开机固定跑满 6 轮 ≈ +4.5s 启动时间，换来确定性：面板最后状态 = 稳定电源下的全新初始化。
- 新增 bootAttempts()/bootId() 接口 + printInfo 打印 "[lcd] boot: attempts=N id=..."，冷插后发 info 即可查历史，不再需要抢插线瞬间抓串口。
- 踩坑：重试环 for(int attempt) 作用域出循环后不可见 → 编译失败但 flash 脚本照烧旧产物（BUILD_EXIT=1 FLASH_EXIT=0 的假成功），修复为循环外声明后重烧成功。
- 串口全绿：WiFi/NTP 13:25:16/天气 971 bytes。**冷插复测待用户执行**（当前固件已含全部保险）。
## 2026-09-17 13:50 冷插雪花排查：外设嫌疑排除，进入 lcd_min 对照实验
- 隔离实验：外设（传感器/马达/喇叭）全拔后冷插 mood_clock，串口确认 6 轮保险环完整跑完、系统全绿，**屏幕仍雪花** → 上电时序假设 + 外设拖垮供电假设双双出局。
- 剩余两假设：①硬件状态变化（昨天至今反复插拔，面板/排线可能受损）；②mood_clock 扩展初始化序列（B2/B7/BB/C0/C6/D0/伽马，比 lcd_min 最小序列多一大段）在冷面板上闯祸——但理论上第 2~6 轮是"热面板重初始化"，应该能落位，所以 ② 的机制存疑。
- 决定性实验：烧 lcd_min（外设仍全拔）→ 用户冷插。lcd_min 干净=固件差异，需二分定位；lcd_min 也雪花=硬件问题。
- 关键未验证前提浮出：lcd_min"冷插干净"的确认发生在昨天、外设接线之前，今天的接线/硬件状态下未复测过。
## 2026-09-17 13:59 ★真凶定罪：重试环"假断电"倒灌自毁，改单次干净上电
- lcd_min 外设全拔冷插干净（用户确认色条正常）→ 硬件无恙，差异锁定在固件。
- 根因闭环：mood_clock v2 的 6 轮重试环里，"断电 300ms"期间 CS/DC/WR/RD 仍驱动为高，电流经面板 IO 保护二极管倒灌供电，面板从未真正失电、锁死脏状态；第 1 轮本已成功（等价 lcd_min 干净时序），反被后面 5 轮毁掉。
- 终版方案：砍掉重试环，单次干净上电 = delay(300) 稳定等待 + 全脚预压（RST 低）+ powerOn(100ms) + runInit 一次，与 lcd_min 验证过的冷插流程完全一致。保留 bootAttempts/bootId 记录（attempts 恒为 1）。
- 编译烧录成功（v11）。冷插复测待用户执行；通过后再逐个接回外设验证。
## 2026-09-17 14:18 冷插雪花二分实验：runInit 换 lcd_min 最小序列（v12）
- v11 单次干净上电冷插仍雪花 → 上电时序假设全面出局，嫌疑收敛到初始化命令序列内容（三个雪花版全带扩展命令段 B2/B7/BB/C0/C6/D0/双3A/伽马，lcd_min 最小序列始终干净）。
- v12：runInit 重写为 lcd_min panelInit 逐拍同款——完整 RST 脉冲 H20→L20→H120、digitalWrite 定 CS/DC/WR（删寄存器直写空脉冲）、最小命令序列（01/11/3A/36/C2/C3/C4/21/13/29），仅保留应用必需的 MADCTL 0x60（横屏）。applyGeometry 移到 begin()。begin 其余（预压脚/300ms/powerOn/readId 记录）不动。
- 编译烧录成功（BUILD_EXIT=0 FLASH_EXIT=0），热启动全绿：WiFi/NTP 14:18:26/天气 918B。**冷插复测待用户执行**。
- 判读：干净=扩展序列定罪（后续如需伽马校色再逐条加回并冷插验证）；仍雪花=序列无罪，下一层嫌疑=CS 常低（lcd_min 每字节拉 CS）或 readIdRaw 的 RD 翻转/脚方向切换。
## 2026-09-17 14:28 v13：begin() 冷启动路径整体克隆 lcd_min
- v12（lcd_min 最小序列 + CS 常低 + 预压脚 + readId）冷插仍雪花 → 序列内容定罪失败。
- v13 结构性清零剩余差异：供电先行（RST 上电窗口浮空）→ pinMode 后配 → 背光直亮 → 每字节翻转 CS 的 lmWr/lmCmd/lmData 独立写函数发最小序列（含末尾 MADCTL 0x60 切横屏）→ 全程零读操作（去 readIdRaw）。runInit 保留给 reinit()。
- 编译烧录成功（BUILD_EXIT=0 FLASH_EXIT=0）。**冷插复测待用户执行**。
- 判读：干净=剩余结构性差异之一（CS 常低/预压脚/readId）为根因，可再逐项加回定位；仍雪花=mood_clock 与 lcd_min 只剩运行期差异（WiFi/外设/帧缓冲推送），嫌疑转向运行期行为对冷面板的影响。
## 2026-09-17 14:49 ★决定性证据：REINIT 救不活雪花 → v14 真断电重启上线
- 活体诊断：雪花现场串口发 REINIT（runInit 整套重初始化含 RST 硬件脉冲 + fillScreen），面板仍雪花 → 面板逻辑锁死，复位脉冲都不认。用户确认外设线未接（排除外设变量）。
- 推论：v11-v13 冷插雪花后，热启动（烧录重启）屏幕大概率也是雪花（面板状态继承），而 09:50 版热启动干净是因为此前 lcd_min/测试固件刚跑过、面板恰在好状态。冷热差异其实是"面板当前状态"差异，不是固件时序差异。
- v14：panelHardRestart()——供电拉低 + 全部 LCD 脚（RST/CS/DC/WR/RD/BL/数据）转 INPUT 高阻（杜绝 v2 假断电的二极管倒灌）+ 1s 彻底失电 + lcd_min 逐拍克隆上电初始化。begin() 与 reinit() 均走此路径（REINIT 从此可救活锁死面板）。
- 编译烧录成功，系统全绿。**判决点：烧录前面板正雪花，v14 启动已对其执行真断电循环——若屏幕此刻恢复，机制实锤且修复生效；冷插复测待用户。**
## 2026-09-17 15:02 v14 真断电也救不回冷插雪花 → 硬件劣化嫌疑升级，重测 lcd_min 基准
- 用户补充关键信息：上午屏幕正常（能变色/条纹），断电重开就雪花 → 面板非坏死，但"断电再上电"动作触发雪花样态。
- v14 真断电循环（1s 失电+高阻+lcd_min 克隆初始化）冷插仍雪花 → 连真断电都救不回，超出固件可解释范围，面板/排线劣化嫌疑升级（今天反复插拔+多次雪花态+假断电倒灌史）。
- 已重烧 lcd_min（09:29 产物，15:02）做基准复测：lcd_min 上次冷插干净是 13:52，若现在也雪花=硬件确认劣化（查排线/焊点/换线换口）；若仍干净=固件差异论复活，终极克隆（mood_clock setup 里先 LCD 后一切）再战。
## 2026-09-17 15:27 v15：LCD 优先初始化 + 功能核对（用户需求已全部在位）
- 决定性对照完成：lcd_min 冷插干净（15:12 热/15:22 冷都正常）→ 硬件无罪，雪花锁定在 mood_clock 固件运行期差异。
- 用户提出正式需求：两页界面——①温和卡通+时间+天气+心率；②心情选色+卡通表情（伤心=紫、开心=粉）。核对发现 v14 已全部实现：HOME 页（时间/日期/心率+心形/天气+湿度/太阳月亮云卡通）+ FEEL 页（开心粉/伤心紫流泪/生气红/平静蓝 + 表情脸），BOOT 键翻页、摇晃换心情。
- v15 改动：setup() 里 tft.begin() 提到第一步（Serial 后立即点屏，lcd_min 同款结构），外设/显存/网络全部后移。驱动保留 panelHardRestart（真断电循环，REINIT 可救活）。
- 编译烧录成功。待用户验证：热启动画面 + 冷插是否仍雪花。若 v15 冷插仍雪花而 lcd_min 干净 → 剩余嫌疑只有运行期行为（显存/网络/刷新对冷面板的影响）。
## 2026-09-17 15:40 接线后重启：屏幕+传感器全部就位
- 用户重新接好外设线后板子串口完全静默（被动监听 10s 零字节，INFO 无响应）→ 重烧 v15 重启（flash_fast.py MAC 认板，FLASH_EXIT=0）。
- 启动日志全绿：[boot] lcd first done（LCD 优先初始化生效）、[io] shake=P2 motor=P1 speaker=P12 btn=P0、[gfx] fb ready heap=311420 psram=8275500、WiFi 192.168.110.134、NTP 15:38:50、天气 916B。
- INFO 复核（15:40:34）：stage=2、wifi=1、time ready=1、weather valid 27.4C 75% aqi34 d01、theme=1（心情索引1）、lcd attempts=1 id=00 00 00（零读路径）。
- 传感器验证判据已确认：心情页摇晃→串口打 [feel] 心情名+换心情（开心粉/伤心紫/生气红/平静蓝）；主页摇晃→心率+2 并立即重画。
- 待用户实测：摇晃看屏幕心率变化/心情页切换。
## 2026-09-17 15:50 ★雪花元凶浮出：马达/蜂鸣反馈拉垮共轨供电（运行中雪花）
- 新证据：v15 运行数分钟后屏幕变雪花，且串口被动监听时抓到板子**自发重启**（完整开机横幅）——马达 4s 长震（ACK_VIB_N_HOME=40 脉冲）+ 4Ω 喇叭蜂鸣与 LCD 共用 GPIO15 控制的 3V 轨，电流尖峰既锁死面板又可能触发 ESP32 brownout。时间线吻合：用户按提示摇了一下（触发 4s 震动）后立即雪花。
- 此前 lcd_min 冷插干净全部发生在外设未接状态；"外设嫌疑排除"结论仅对冷插成立，对运行期不成立。
- v16：加 s_ackEnable 总开关 + 串口命令 VIBOFF/VIBON（关时立即停马达/蜂鸣）。已烧录并发 VIBOFF（[cmd] ack feedback = OFF）。
- 待验证：VIBOFF 状态下摇晃（心率应+2、心情页应换色但无震动）→ 屏幕不再雪花 = 实锤马达电流；硬件解法=马达/喇叭改三极管驱动或独立供电。
## 2026-09-17 15:58 v17：震动反馈默认关闭 + 崩溃链闭环
- 用户实测：摇了板子马达仍震且屏幕仍雪花 → VIBOFF 没保住 = 板子在我关掉后又崩掉重启过（开关被重置）。铁证：马达+蜂鸣一响板子就崩（brownout 掉电复位）。
- 崩溃链闭环：摇晃 → 马达4s+蜂鸣拉电流 → ESP32 掉电复位 → 复位瞬间 GPIO15 掉电、面板脏上电 → mood_clock 冷启动初始化 → 雪花。用户看到的雪花=每次崩溃重启后的冷启动雪花。
- v17：s_ackEnable 默认 false（重启不回弹），INFO 增加 ack feedback 状态行。已烧录，启动全绿（15:57:43 NTP，天气 27.0C 78%），INFO 确认 ack feedback: OFF。
- 待验证：关反馈后摇晃（心率+2、无震动无蜂鸣）屏幕是否稳定。稳定=实锤共轨过载；硬件解法=马达/喇叭加三极管（如 S8050/PN2222 基极串 1k）或独立供电，蜂鸣走功放/限流。
## 2026-09-17 16:03 未检验变量浮出：15:43 的重新接线本身
- v17（震动默认关）后屏幕仍雪花。重新审视时间线：用户 15:43 重新接线，此后所有雪花都在新接线状态下；lcd_min 冷插干净的基准（15:12/15:22）是旧接线状态测的。若重接有错（马达并到电源轨/喇叭跨在电源两端等），会持续拖垮共轨 → 屏幕永远雪花 + 板子崩溃重启，与固件无关。
- 已烧回 lcd_min（16:03）做新接线基准。计划二分：①全拔冷插看 lcd_min（新接线拆除后基准）；②烧 mood_clock v17 全拔冷插；③逐个接回（传感器→马达→喇叭）每步冷插，定位元凶接线。
## 2026-09-17 16:15 二分进行中：全拔 lcd_min 干净，mood_clock v17 已烧回
- 第 1 步结果：用户报告屏幕不再是雪花（lcd_min 显示正常）→ 主板+屏幕当前健康。'晃动没反应'属预期（lcd_min 无摇晃逻辑）。
- 已烧回 mood_clock v17（16:15），INFO 全绿：stage=2、天气 26.6C 78%、ack feedback OFF。待用户回报：①当前外设接线状态；②屏幕是否时钟页；③摇晃心率是否+2（注意：震动默认关，唯一可见反应=心率数字变化，别把'不震'当成'没反应'）。
## 2026-09-17 16:30 功能验证通过：BOOT+摇晃全活；心情页改高饱和配色（v18/v19）
- 串口实锤：[page] feel + 两次 [feel] → BOOT 键、摇晃传感器、心情切换逻辑全部正常工作。
- '灰黑/蓝色不动'根因=原心情页四色全是低饱和粉彩（开心粉≈0xFEFD 近白），在屏幕上显不出颜色。
- v19：kSkin 改高饱和——开心亮粉 0xFDAB/0xF9EF、伤心紫 0xBABA/0x8010、生气红 0xD8A7/0x8800、平静蓝 0x4416/0x18CE；心情名与提示文字改白色（原深色字在饱和底上不可见）。编译烧录成功。
- 待用户确认：按 BOOT 看心情页颜色是否鲜明，摇晃换色。
## 2026-09-17 16:39 摇一摇"闪七个颜色"根因+修复（v20 摇晃聚合）
- 症状：心情页摇一下，四色快速轮播停不下来。根因=SHAKE_DEBOUNCE_MS 100ms 挡不住倾斜开关的连续弹跳，一次摇晃触发 5~10 次 onShake → 心情连切。
- 修复（摇晃聚合）：弹跳期只记 pulses，稳定 500ms 后统一 applyShake 一次 —— 一次摇晃=一次心情切换；串口 [feel] 日志带 pulses=N 可验证聚合效果。onShake 保留为串口 SHAKE 命令的模拟入口。编译烧录成功（v20）。
- 当前功能全貌：BOOT 翻页正常、摇晃换心情（高饱和粉/紫/红/蓝）、心率+2/次、震动蜂鸣默认关（马达待加三极管）。
## 2026-09-17 17:02 v21「开机必显示」：时钟页先行 + WiFi 延迟启动 + 降射频功率
- 策略转向：7/7 mood_clock 冷插雪花 vs 3/3 lcd_min 干净，指令级克隆仍雪花 → 剩余最大嫌疑 = WiFi 射频启动（电流冲击/总线活动），直接按"开机就能显示"做稳定性加固而非继续二分。
- 改动：①新增 ST_IDLE 开机静默期——setup 里画完整时钟页（占位时间，NTP 后自动重画）并 gfx_flushAll 一次，之后画面零活动，6 秒后才 netStartWiFi；静默期外设（摇晃/按键）照常轮询。②netStartWiFi 加 WiFi.setTxPower(WIFI_POWER_11dBm)（默认 19.5dBm 的一半多），减射频电流尖峰。③开机不再有"连接网络"状态页的每 600ms 全帧重刷动画。
- 编译烧录成功，日志全绿：lcd first → fb ready → font OK → deferred start → connected 192.168.110.134 → NTP 17:02:34 → weather 902B。低功率下 WiFi 照常连上。
- 冷插复测待用户。若仍雪花，二分下一刀=PSRAM 显存（lcd_min 不用 PSRAM 帧缓冲，mood_clock 170*320*2B=108KB 在 PSRAM）。
## 2026-09-17 17:19 v22 开机自愈环（4 轮真断电）+ 编译配置排除
- 指纹对比：build_lcdmin 与 build_moodclock 的 bootloader/分区表 MD5 完全一致 → FQBN/PSRAM/Flash 配置差异彻底排除，两固件唯一差异只剩 app 代码层。
- 决定性规律收敛：面板"由固件主动上电"（烧录重启，面板先断电十几秒）→ 每次都亮（v17/v19/v20/v21 烧完全部正常显示）；面板跟着 USB 电压爬坡（重插线）→ 经常雪花且锁死。面板上电成功率有随机性。
- v22 自愈环：begin() 里连做 4 轮 panelHardRestart（首轮断电 3s、后续 2s；引脚全高阻防倒灌；每轮结束 fillScreen 黑帧压总线）。WiFi 延迟缩到 3s（自愈环已占 12s）。代价开机 ~15s，换确定性。
- 编译烧录成功，日志确认 4 轮完整跑完 + deferred wifi + NTP 17:19:38 + weather 901B。冷插复测待用户；若仍雪花 → 面板硬件边际失效实锤（今日反复折腾），转硬件路线：静置/检查排线/换线换口。
## 2026-09-17 17:58 v22 自愈环失败 → 补测 lcd_min 新接线基准（最后缺失的数据点）
- v22（4 轮真断电自愈）冷插仍雪花 → "断电 2-3s 不足"假设未验证成功，且发现关键盲区：lcd_min 冷插干净的全部记录都在 15:43 重新接线之前；重接传感器线后 lcd_min 从未冷插复测过。当前雪花的所有样本都带"新接线"变量。
- 已烧 lcd_min（17:58，FLASH_EXIT=0）做基准复测：lcd_min 冷插干净（当前接线）→ 差异仍在固件，下一刀=断电时长 15s（复刻烧录窗口的面板失电时长，烧录重启百发百中）；lcd_min 也雪花 → 面板硬件边际失效实锤，转硬件路线（静置/排线/换线）。
## 2026-09-17 18:14 v23 ★新主嫌疑：PSRAM 帧缓冲（雪花=推出去了坏数据）
- lcd_min 当前接线冷插干净（18:05 用户确认"现在正常"）→ 硬件无罪最终确认。
- 新理论首次完美覆盖所有现象：mood_clock 画面=PSRAM 帧缓冲（108KB@8MB OPI）整帧推出，lcd_min 零 PSRAM 直画。若冷插后 PSRAM 位不稳 → 推出整屏随机噪声=雪花，且重画/REINIT/自愈环全部无效（每次都读同一块坏缓冲）→ 解释 REINIT 没用、雪花纹丝不动、热启动正常、lcd_min 永远干净、自愈环无效全部现象。
- v23：gfx_begin 改内部 SRAM 优先（heap_caps MALLOC_CAP_INTERNAL，106KB@0x3FCAB060，堆余 200KB；明文 HTTP 无 TLS 无压力）；自愈环收敛回 1 轮 1s；setup 首帧后打印 fb checksum（冷插后对比热启动值 0BE91584，若变化=缓冲坏实锤）。
- 编译烧录成功，热启动全绿 NTP 18:14:07。冷插复测待用户。
## 2026-09-17 18:50 v24 ★最后未对齐差异：写路径 CS 常低 → 每字节翻转
- v23（fb 挪内部 SRAM）冷插仍雪花 → PSRAM 理论出局。
- 复查写路径发现：v13 只克隆了 lcd_min 的**初始化**（lmCmd/lmData 每字节翻转 CS），但之后所有绘图（fillRect/pushImage/setWindow 的命令流）仍走 cmd()/data8() 的 **CS 常低 + WR 寄存器直写**语义——这条数据流从未被 lcd_min 化。lcd_min 冷插干净的全部记录都是每字节翻转 CS（writeCmd/writeData），7/7 雪花 vs N/N 干净之间最后一个结构性差异。
- v24：busWrite 的 WR 改 digitalWrite；cmd/data8 改为每字节翻转 CS（与 lmCmd/lmData 逐拍同款）；panelHardRestart 末尾 CS 保持 HIGH；fillRect/pushImage 像素流改走 data8（每字节翻转 CS）；readIdRaw/readId/readPixel 读事务显式压 CS。编译烧录成功（BUILD_EXIT=0），热启动全绿：fb checksum=0BE91584（与基准一致）、heap 200KB、NTP 18:44:35。
- 新增串口诊断命令：SUM（实时重算 fb 校验和，对比基准 0BE91584 判 fb 内容好坏）、PAT（tft.testPattern 绕过 fb 直写面板，判直写路径通不通）。
- 后台挂 listen.py 等 30 分钟，冷插后自动抓串口（含开机 fb checksum）。判读表：冷插干净=结案；仍雪花+SUM 一致+PAT 雪花=面板状态问题（软件路径已全对齐，转硬件/换板）；仍雪花+SUM 变化=fb 内存位不稳。
## 2026-09-17 19:30 mood_clock → openVela(NuttX) 源码级移植完成
- 用户要求把当前 Arduino 固件转换成 open-vela 代码模型（github.com/open-vela，NuttX 内核，dev-ai-contest-2026）。
- 产出 `openvela_port/`：moodclock app 包（moodclock_main.c/tds3_st7789.c/mc_gfx.c/mc_net.c/mc_gpio.c/mc_compat.h + Kconfig/Makefile/Make.defs，字库 glyphs.h/theme.h 原样复制）+ README_迁移指南.md。
- 关键移植点：setup/loop→moodclock_main 单任务；Serial→printf+stdin poll 非阻塞（getchar 会卡死主循环）；millis/delay→CLOCK_MONOTONIC/nxsig_usleep；delayMicroseconds→up_udelay（时序关键不能 tick 级）；GPIO 集中适配 mc_gpio.c（esp32s3 HAL 签名随内核版本变，全包唯一适配点）；v24 驱动结论（每字节翻转 CS/真断电/行偏移35/C3=0x02）全保留；WiFi/NTP/天气为离线骨架（mc_net.c TODO：WAPI/ntpc/webclient），离线 3 轮失败自动进运行态显示 --:-- 不卡状态页。
- 诚实边界：本 Windows 机无法编译 openVela（官方仅支持 Ubuntu 22.04 原生，WSL/Docker 不支持），移植是源码级+框架注册，编译验证要在 Ubuntu 树里做；蜂鸣 PWM 未移植（马达反馈保留）。
- 环境注意：本会话后期 PowerShell stdout 全程异常（读输出需落盘+Read），python/node 子进程一度执行失败——arduino-cli/flash 链路不受影响。
- ★冷插复测（v24 CS 每字节翻转版）用户仍未回报，cold_listen.py 监听器在后台等拔插。
- ★ 19:39 冷插复测成功，雪花屏问题结案：真凶 = mood_clock 绘图路径 CS 常低 + WR 寄存器直写（lcd_min 每字节翻转 CS 永远干净）。v24 全写路径对齐 lcd_min 后冷插一次点亮（boot attempts=1），屏幕显示时间/心率/温度，BOOT 翻页正常。冷启动日志 fb checksum=0BE91584 与基准一致；INFO id=00 00 00（RDDID 读回异常但显示正常，不阻塞）。串口证据：logs/cold_v24_1_20260917_193932.log
- ★ 19:46 复测（第 2 次冷插）再次全绿：boot attempts=1、fb checksum=0BE91584 与基准一致、NTP 19:46:43、天气 25.7C 已取、stage=3 暮色主题。连续两次冷插干净，v24 修复（每字节翻转 CS）正式结案。注意：18:48 启动的 cold_listen.py 老监听器仍在后台值班（最多 3 轮），新挂的第二实例因抢口 PermissionError 退出属预期。证据：logs/cold_v24_2_20260917_194712.log
- 20:33 摇晃传感器排查结案：90s 串口窗口抓到 14 次 [feel] 切换（伤心/生气/平静/开心全轮到），传感器+固件链路全通。用户所谓『没反应』= ①时钟页摇晃只+2心率不明显 ②马达反馈 v17 起默认关（VIBOFF，防共轨雪花复发）。如需震动反馈可串口 VIBON（有屏幕风险，未开）。
- 20:42 v25 烧录成功：s_ackEnable 默认 true（用户要求摇晃=马达+喇叭反馈）。烧后验证 ack feedback: ON、屏幕正常（fb checksum=0BE91584）、NTP/天气全绿，期间 [feel] 切换正常。雪花未复发（持续观察）。回退方案：串口 VIBOFF 即关，无需重烧。
